import { Container, Input } from "@chakra-ui/react";

const Transaction = () => {
  return (
  <Container bg="black">
    <Input placeholder="Date">
    </Input>
  </Container>);
};

export default Transaction;
